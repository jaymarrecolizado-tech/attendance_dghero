================================================================================
digitalhero.dictr2.cloud — QUICK DEPLOY (All Father)
================================================================================

STEP 1: UPLOAD FILES
--------------------
Upload ALL files from PROJECT ROOT (not TODODEPLOYMENT/uploads/ — folder
not in repo) to public_html / ~/domains/digitalhero.dictr2.cloud/public_html.
Exclude: see TODODEPLOYMENT/DO_NOT_UPLOAD.txt (diagnose.php, create_admin.php, etc.)
Use: SFTP/SCP, File Manager, or SSH.

STEP 2: DATABASE
-----------------------
DB already exists on 187.77.150.203:
  Name: dbdigitalhero  User: dbudigitalhero  Host: localhost
If recreating: hPanel → Databases → MySQL Databases → ALL PRIVILEGES.

STEP 3: CONFIGURE ENVIRONMENT
------------------------------
1. In public_html, copy env.example to .env:
   cp env.example .env
   
2. Edit .env file with your actual credentials:
   DB_HOST=localhost
   DB_NAME=dbdigitalhero
   DB_USER=dbudigitalhero
   DB_PASS=your_database_password  # from TODODEPLOYMENT/.env.production
   DB_AUTO_MIGRATE=false
   RATE_LIMITER_DRIVER=file
   APP_DEBUG=false
   TZ=Asia/Manila
   
   SMTP_HOST=smtp.hostinger.com
   SMTP_PORT=465
   SMTP_USER=your_email@digitalhero.dictr2.cloud
   SMTP_PASS=your_email_password
   SMTP_SECURE=ssl
   SMTP_FROM=your_email@digitalhero.dictr2.cloud
   
   APP_URL=https://digitalhero.dictr2.cloud
   CSP_STRICT=false
3. On server use TODODEPLOYMENT/.htaccess.production as .htaccess (HTTPS).

STEP 4: RUN MIGRATIONS
----------------------
Option A - Via SSH (Recommended):
  cd ~/public_html  # or ~/domains/digitalhero.dictr2.cloud/public_html
  php scripts/run_migrations.php

Option B - Via Browser (Temporary):
  1. Create file: run_migrations_once.php
  2. Add this code:
     <?php
     require __DIR__ . '/config/bootstrap.php';
     require __DIR__ . '/vendor/autoload.php';
     \App\Services\Database::migrate();
     echo "Migrations completed!";
  3. Visit: https://digitalhero.dictr2.cloud/run_migrations_once.php
  4. DELETE the file immediately after!

STEP 5: CREATE ADMIN USER
--------------------------
Option A - Via SSH:
  php scripts/seed_admin.php admin your_secure_password

Option B - Via Browser (Temporary, then DELETE):
  Use create_admin.php only if needed — gated to CLI or APP_DEBUG+localhost.
  Prefer SSH.

STEP 6: SET PERMISSIONS
-----------------------
  bash TODODEPLOYMENT/set_permissions.sh  # or bash set_permissions.sh
Sets: dirs 755, files 644, .env 600, storage/* writable.

STEP 7: TEST
------------
1. Visit: https://digitalhero.dictr2.cloud/?r=register
2. Test registration with ?r=register&e=your-event-slug
3. Login as admin: ?r=admin_login
4. Configure SMTP: ?r=admin_settings
5. Test scan: ?r=scan&e=your-event-slug

STEP 8: SECURITY
----------------
1. Ensure .env file permissions: chmod 600 .env
2. Delete diagnose.php / create_admin.php if uploaded
3. Enable HTTPS/SSL (already in .htaccess.production)
4. DB_AUTO_MIGRATE=false on server
5. Keep .env out of git

================================================================================
For detailed instructions, see DEPLOYMENT.md + TODODEPLOYMENT/README.md
================================================================================

